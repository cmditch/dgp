<%= form_for @vendor, :html => { :class => "form-horizontal vendor" } do |f| %>

  <% if @vendor.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@vendor.errors.count, "error") %> prohibited this vendor from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @vendor.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :encrypted_bitpay_pem, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :encrypted_bitpay_pem, :class => 'form-control' %>
    </div>
    <%=f.error_span(:encrypted_bitpay_pem) %>
  </div>
  <div class="form-group">
    <%= f.label :encrypted_bitpay_pem_iv, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :encrypted_bitpay_pem_iv, :class => 'form-control' %>
    </div>
    <%=f.error_span(:encrypted_bitpay_pem_iv) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                vendors_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
